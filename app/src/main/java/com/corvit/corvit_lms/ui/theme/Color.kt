package com.corvit.corvit_lms.ui.theme

import androidx.compose.ui.graphics.Color

// Default Material Colors (Unused but kept for reference)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// --- CORVIT BRAND COLORS ---
val CorvitPrimaryRed = Color(0xFFBB2233)
val CorvitPrimaryRedDark = Color(0xFFD32F2F) // Slightly brighter for dark mode visibility

// Semantic Colors
val CorvitSuccessGreen = Color(0xFF4CAF50)
val CorvitWarningYellow = Color(0xFFFFC107)
val CorvitErrorRed = Color(0xFFF44336)
val CorvitInfoBlue = Color(0xFF2196F3)

// --- LIGHT THEME COLORS ---
val LightBackground = Color(0xFFF8F9FA) // Soft off-white
val LightSurface = Color(0xFFFFFFFF)     // Pure white cards
val LightSurfaceVariant = Color(0xFFF1F3F5) // Light gray for segmented controls/secondary cards
val LightOnBackground = Color(0xFF212529)   // Near black text
val LightOnSurface = Color(0xFF343A40)      // Dark gray text
val LightOnSurfaceVariant = Color(0xFF495057) // Medium gray text
val LightOutline = Color(0xFFDEE2E6)        // Light gray borders

// --- DARK THEME COLORS ---
val DarkBackground = Color(0xFF121212)      // Standard Material dark background
val DarkSurface = Color(0xFF1E1E1E)         // Slightly lighter than background for cards
val DarkSurfaceVariant = Color(0xFF2C2C2C)  // Lighter still for secondary elements
val DarkOnBackground = Color(0xFFE0E0E0)    // Off-white text
val DarkOnSurface = Color(0xFFEEEEEE)       // Brighter white for primary text
val DarkOnSurfaceVariant = Color(0xFFBDBDBD) // Light gray for secondary text
val DarkOutline = Color(0xFF424242)         // Dark gray borders